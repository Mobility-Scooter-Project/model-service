# This placeholder stack intentionally declares only the Terraform core version.
# We are documenting the target contract here, not yet installing real OpenStack
# providers or credentials.
terraform {
  required_version = ">= 1.5.0"
}
