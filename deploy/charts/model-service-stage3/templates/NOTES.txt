Stage 3 installs one release per model image.

The external route for this release is:
  http://{{ .Values.host }}{{ .Values.pathPrefix }}/jobs

This release creates:
  - always-on API deployment: {{ include "model-service-stage3.apiName" . }}
  - queue worker deployment: {{ include "model-service-stage3.workerName" . }}
  - Redis-scaled worker queue: {{ include "model-service-stage3.queueName" . }}
